---
description: 
---

Use the lint-and-validate skill on all changed files first.
If lint passes, write a git commit message for the changes.
Format: <type>(<area>): <description>. Under 72 characters. Present tense.
// turbo
git add . && git commit -m "<generated message>"
